import React from "react";
import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(({}) => ({}));

export const JobFeed = ({}) => {
  const classes = useStyles();

  return <div style={{ color: "red" }}>YOUR JOB FEED WIP</div>;
};
